abi <abi/4.0>,

include <tunables/global>

/usr/bin/tiny_runtime flags=(default_allow) {
  userns,

  # Site-specific additions and overrides. See local/README for details.
  include if exists <local/tiny_runtime>
}

